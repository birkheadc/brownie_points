<header>
  <%= link_to (image_tag 'favicon.ico', :alt => 'B'), root_path, class:'icon-link' %>
  <%= render :partial => 'nav/nav-logged-out' unless logged_in? %>
  <%= render :partial => 'nav/nav-logged-in' if logged_in? %>
</header>